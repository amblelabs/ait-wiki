title: Custom Categories
blocks:
  - _type: callout
    type: warning
    value: You need to learn how texture pack and datapacks works!
  - _type: text
    alt: Content
    value: "## Create a Datapack"
  - _type: callout
    type: info
    value: Follow the tutorial on the [Minecraft Wiki on
      datapacks](https://minecraft.wiki/w/Data_pack) on how to set one up, use
      the namespace from earlier.
  - _type: text
    alt: Content
    value: |-
      Create a new **.json** file in the path

      > `data/(namespace)/categories/(category_name).json`
      > 
      > *   This will contain the "code" for the categorie.
      >     
      > 
      > Inside this new .json file, paste:

      > ```json
      > {
      >    "id": "namespace:category_name",
      >   "name": "category_name"
      > }
      > ```
sidebar:
  open: true
  exclude: false
